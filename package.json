{
  "name": "cocopit",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "web",
    "shared"
  ],
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "build": "bun run --cwd web build",
    "start": "bun server/index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  },
  "trustedDependencies": [
    "node-pty"
  ]
}
